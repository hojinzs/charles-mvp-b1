# Database
DATABASE_URL=postgresql://crawler:crawler123@postgres:5432/crawler_db

# Redis
REDIS_URL=redis://redis:6379

# API Server
PORT=3000
NODE_ENV=development

# Worker
WORKER_CONCURRENCY=2

# Scheduler
# Cron expression for scheduling (default: every minute)
SCHEDULER_CRON="* * * * *"
# Interval threshold in ms (keywords checked if older than this)
SCEDULER_INTERVAL_MS=60000

# Puppeteer (Optional: Use Local Chrome on macOS to avoid crash)
# PUPPETEER_EXECUTABLE_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
